// Remember, we're gonna use strict mode in all scripts now!
"use strict";

// console.log("live-server test");
/* 
const arrFirstMeasurement = [17, 21, 23];
const arrSecondMeasurement = [12, 5, -5, 0, 4];
const arrTotalMeasurement = arrFirstMeasurement.concat(arrSecondMeasurement);

console.log(arrTotalMeasurement);

let str = "";
for (let i = 0; i < arrTotalMeasurement.length; i++) {
  str += `The temperature measured in day ${i + 1} is ${
    arrTotalMeasurement[i]
  } celsius,`;
}

console.log(str);
 */
